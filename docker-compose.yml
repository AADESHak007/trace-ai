# Docker Config for Redis

version: '3.8'

services:
  redis:
    image : redis:alpine
    container_name: trace-ai_redis
    ports:
      - "6379:6379"
    volumes:
      - trace-ai_redis_data:/data
    restart: always
  
volumes:
  trace-ai_redis_data: